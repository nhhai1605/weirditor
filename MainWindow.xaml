<Window x:Class="weirditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:weirditor"
        xmlns:viewModels="clr-namespace:weirditor.ViewModels"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="weirditor"
        Height="600"
        Width="800">
    <Window.DataContext>
        <viewModels:MainWindowViewModel />
    </Window.DataContext>
    <DockPanel Margin="1">
        <Menu DockPanel.Dock="Top">
            <Menu.Items>
                <MenuItem Header="File" DataContext="{Binding FileView}">
                    <MenuItem Header="New" Command="{Binding NewCommand}" />
                    <MenuItem Header="Open" Command="{Binding OpenCommand}" />
                    <MenuItem Header="Save" Command="{Binding SaveCommand}" />
                    <MenuItem Header="Save as..." Command="{Binding SaveAsCommand}" />
                </MenuItem>
                <MenuItem Header="Setting" DataContext="{Binding DocumentView}">
                    <MenuItem Header="Format" Command="{Binding FormatCommand}" />
                    <MenuItem Header="Word Wrap" Command="{Binding WrapCommand}" IsChecked="{Binding Format.Wrap}" />
                </MenuItem>
            </Menu.Items>
        </Menu>
        <DockPanel>
            <TextBlock DockPanel.Dock="Bottom"
               DataContext="{Binding DocumentView}"
               Text="{Binding Document.FilePath}"
               FontSize="12"
               FontFamily="{Binding Format.Family}"
            />
            <avalonEdit:TextEditor
                xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
                Name="TextEditor"
                FontFamily="{Binding Format.Family}"
                DataContext="{Binding DocumentView}"
                FontSize="{Binding Format.Size}"
                FontStyle="{Binding Format.Style}"
                FontWeight="{Binding Format.Weight}"
                ShowLineNumbers="True"
                LineNumbersForeground="Gray"
                WordWrap="{Binding Format.Wrap}"
            />
        </DockPanel>
    </DockPanel>
</Window>